import course from 'ember-fundamentals/utils/course';
import { module, test } from 'qunit';

module('Unit | Utility | course', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = course();
    assert.ok(result);
  });
});
